# 🌐 웹 대시보드 사용 가이드

## 개요

웹 대시보드는 기존 터미널 기반 모니터링을 브라우저에서 시각적으로 확인할 수 있게 해주는 기능입니다.

## 🚀 빠른 시작

### 1. 웹 대시보드 실행
```bash
# 방법 1: 통합 실행
python main.py --mode dashboard

# 방법 2: 직접 실행
python web_dashboard.py
```

### 2. 브라우저 접속
- **URL**: http://localhost:5000
- **포트**: 5000 (기본값)

### 3. 로그 생성 (다른 터미널에서)
```bash
python main.py --mode generate
```

## 📊 대시보드 기능

### 1. 실시간 통계 카드
- **총 로그 수**: 전체 처리된 로그 개수
- **에러 로그 수**: 에러로 분류된 로그 개수  
- **에러율**: 에러 로그 비율 (%)
- **발견된 패턴**: 클러스터링된 에러 패턴 개수

### 2. 최근 로그 패널
- 실시간으로 들어오는 로그 표시
- 에러/정보 로그 구분 표시
- 타임스탬프와 함께 표시
- 최근 10개 로그만 표시

### 3. 에러 패턴 클러스터
- Drain3로 클러스터링된 에러 패턴들
- 발생 횟수별 정렬
- 심각도별 색상 구분:
  - 🔴 매우 심각 (50회 이상)
  - 🟠 심각 (20회 이상)  
  - 🟡 주의 (10회 이상)
  - 🟢 경미 (10회 미만)

### 4. 에러 발생 타임라인
- Chart.js를 이용한 실시간 차트
- 시간별 에러 발생 추이
- 실시간 업데이트

## 🔄 실시간 업데이트

### WebSocket 기반 실시간 통신
- **새 에러 감지**: 즉시 대시보드 업데이트
- **빈발 패턴 알림**: 3회 이상 발생시 알림
- **자동 새로고침**: 5초마다 데이터 업데이트

### 알림 시스템
- 빈발 패턴 감지시 우측 상단 알림
- 5초 후 자동 사라짐
- Slack 연동과 동시 작동

## 🎨 UI/UX 특징

### 반응형 디자인
- 모바일/태블릿/데스크톱 지원
- 그리드 레이아웃으로 자동 조정

### 시각적 요소
- 그라데이션 배경
- 카드형 레이아웃
- 호버 효과
- 애니메이션

### 색상 코딩
- 에러 로그: 빨간색 테두리
- 정보 로그: 초록색 테두리
- 심각도별 색상 구분

## 🔧 기술 스택

### 백엔드
- **Flask**: 웹 프레임워크
- **Flask-SocketIO**: WebSocket 통신
- **Threading**: 파일 모니터링

### 프론트엔드
- **HTML5/CSS3**: 기본 구조
- **JavaScript**: 동적 기능
- **Chart.js**: 차트 시각화
- **Socket.IO**: 실시간 통신

## 📱 사용 시나리오

### 1. 개발팀 모니터링
```bash
# 터미널 1: 웹 대시보드 실행
python main.py --mode dashboard

# 터미널 2: 로그 생성
python main.py --mode generate

# 브라우저: http://localhost:5000 접속
```

### 2. 데모/프레젠테이션
- 브라우저에서 시각적으로 모니터링
- 실시간 차트로 효과적인 시연
- 팀원들과 화면 공유 가능

### 3. 원격 모니터링
- 서버에서 대시보드 실행
- 다른 컴퓨터에서 브라우저로 접속
- 네트워크를 통한 원격 모니터링

## ⚙️ 설정 및 커스터마이징

### 포트 변경
```python
# web_dashboard.py에서 수정
socketio.run(app, host='0.0.0.0', port=8080, debug=False)
```

### 호스트 변경
```python
# 외부 접속 허용
socketio.run(app, host='0.0.0.0', port=5000, debug=False)

# 로컬만 허용
socketio.run(app, host='127.0.0.1', port=5000, debug=False)
```

### 로그 디렉토리 변경
```python
# web_dashboard.py에서 수정
watch_dir = r"C:\your\custom\logs\path"
```

## 🐛 트러블슈팅

### 1. 포트 충돌
```bash
# 다른 포트 사용
python web_dashboard.py
# 코드에서 포트 변경 필요
```

### 2. 모듈 import 오류
```bash
# Flask 설치 확인
pip install flask flask-socketio

# requirements.txt 재설치
pip install -r requirements.txt
```

### 3. 브라우저 접속 불가
- 방화벽 설정 확인
- 포트 5000 열기
- localhost 대신 IP 주소 사용

### 4. 실시간 업데이트 안됨
- WebSocket 연결 상태 확인
- 브라우저 개발자 도구에서 오류 확인
- 네트워크 연결 상태 확인

## 🔒 보안 고려사항

### 개발 환경
- 기본적으로 localhost만 접속 허용
- 디버그 모드 비활성화

### 프로덕션 환경
- HTTPS 사용 권장
- 인증 시스템 추가
- 방화벽 설정
- 로그 파일 권한 관리

## 📈 성능 최적화

### 메모리 관리
- 최근 로그 100개만 저장
- 에러 타임라인 50개만 저장
- 클러스터별 최근 로그 10개만 저장

### 업데이트 주기
- 실시간: WebSocket 이벤트
- 주기적: 5초마다 전체 업데이트
- 차트: 실시간 데이터 스트림

## 🚀 향후 개선 계획

### 기능 추가
- [ ] 사용자 인증
- [ ] 대시보드 설정 저장
- [ ] 알림 설정 커스터마이징
- [ ] 로그 필터링
- [ ] 데이터 내보내기

### 성능 개선
- [ ] 데이터베이스 연동
- [ ] 캐싱 시스템
- [ ] 로드 밸런싱
- [ ] 마이크로서비스 분리

### UI/UX 개선
- [ ] 다크 모드
- [ ] 테마 커스터마이징
- [ ] 모바일 앱
- [ ] PWA 지원

## 📞 지원

문제가 발생하면:
1. README.md의 트러블슈팅 섹션 확인
2. GitHub Issues에 문제 보고
3. 개발팀에 문의

---

**웹 대시보드로 더 편리하고 시각적인 로그 모니터링을 경험해보세요!** 🎉
